<UserControl x:Class="CourierSystemWPF.Views.ContractAdd"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CourierSystemWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="128*"/>
            <ColumnDefinition Width="294*"/>
            <ColumnDefinition Width="49*"/>
            <ColumnDefinition Width="329*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="90*"/>
            <RowDefinition Height="265*"/>
            <RowDefinition Height="61*"/>
            <RowDefinition Height="34*"/>
        </Grid.RowDefinitions>

        <Label Content="Add new contract" Background="#00000000" Foreground="#FF1B3D69" FontSize="24" FontFamily="Cascadia Mono SemiBold" VerticalAlignment="Center" HorizontalAlignment="Left" Grid.Column="0" Grid.ColumnSpan="2" Margin="40,0,0,0" Height="38" Width="235"/>

        <StackPanel Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="1">
            <xctk:DateTimePicker Margin="20,20,20,0" Padding="5" Value="{Binding SelectedStartDateTime}"/>
            <xctk:DateTimePicker Margin="20,20,20,0" Padding="5" Value="{Binding SelectedEndDateTime}"/>
            <ComboBox Margin="20,20,20,0" Padding="5"
                ItemsSource="{Binding Clients}" 
                SelectedItem="{Binding SelectedClient}"
                DisplayMemberPath="id"
            />
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="0">
            <Label Content="Start Date:" Margin="0,19,0,0" Padding="5" Background="#00000000" Foreground="#FF3C3A3A" FontSize="14" HorizontalAlignment="Right"/>
            <Label Content="End Date:" Margin="0,19,0,0" Padding="5" Background="#00000000" Foreground="#FF3C3A3A" FontSize="14" HorizontalAlignment="Right"/>
            <Label Content="Client ID:" Margin="0,19,0,0" Padding="5" Background="#00000000" Foreground="#FF3C3A3A" FontSize="14" HorizontalAlignment="Right"/>
        </StackPanel>

        <Label Content="Notes:" Margin="0,19,0,0" Padding="5" Background="#00000000" Foreground="#FF3C3A3A" FontSize="14" HorizontalAlignment="Center" Grid.Column="2" Grid.Row="1" Width="50"/>
        <TextBox 
            AcceptsReturn="True"
            TextWrapping="Wrap"
            VerticalScrollBarVisibility="Auto"
            Text="{Binding Notes}"
            Grid.Row="1"
            Grid.Column="3"
            Margin="18,18,18,18"
        />

        <Button  Command="{Binding AddCommand}" Content="Add"  Grid.Column="1" Grid.Row="2" Margin="15,15,15,15"/>
        <Button  Command="{Binding NewClientCommand}" Content="New Client"  Grid.Column="0" Grid.Row="2" Margin="15,15,15,15"/>
    </Grid>
</UserControl>
