<UserControl x:Class="CourierSystemWPF.Views.Contracts"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CourierSystemWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200*"/>
            <ColumnDefinition Width="625*"/>
        </Grid.ColumnDefinitions>

        <Grid Style="{StaticResource NavigationSidebarStyle}">
            <Grid.RowDefinitions>
                <RowDefinition Height="50*"/>
                <RowDefinition Height="300*"/>
            </Grid.RowDefinitions>

            <!-- Title box -->
            <Grid Margin="8"  >

                <Border CornerRadius="8" BorderBrush="DarkGray" BorderThickness="3" Background="#FF9DD1FF" Grid.ColumnSpan="2" Grid.RowSpan="2">
                    <Border.Effect>
                        <DropShadowEffect Opacity="0.5"/>
                    </Border.Effect>
                </Border>

                <Label Content="Contracts" Grid.Column="1" Grid.Row="1"  Foreground="#2B3A55" FontFamily="Segoe UI" FontSize="17" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
            </Grid>

            <!-- Button List -->
            <StackPanel Grid.Row="1">
                <Button Style="{StaticResource SidebarButtonStyle}" Command="{Binding ViewContractCommand}">
                    View Contracts
                </Button>

                <Button Style="{StaticResource SidebarButtonStyle}" Command="{Binding ViewClientsCommand}">
                    View Clients
                </Button>

                <Button Style="{StaticResource SidebarButtonStyle}" Command="{Binding AddContractCommand}">
                    Add Contract
                </Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Column="1">
            <ContentControl x:Name="MainView" Content="{Binding CurrentSubView}"/>
        </Grid>
    </Grid>
</UserControl>
