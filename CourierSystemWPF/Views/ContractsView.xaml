<UserControl x:Class="CourierSystemWPF.Views.ContractsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CourierSystemWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="229*"/>
            <ColumnDefinition Width="171*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <DataGrid ItemsSource="{Binding Contracts}" SelectedItem="{Binding SelectedItem}" AutoGenerateColumns="False" IsReadOnly="True" EnableRowVirtualization="False">
            <DataGrid.Resources>
                <Style x:Key="WrapTextBlockStyle" TargetType="TextBlock">
                    <Setter Property="TextWrapping" Value="Wrap"/>
                </Style>
            </DataGrid.Resources>
            
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Height" Value="Auto"/>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding id}" 
                            ElementStyle="{StaticResource WrapTextBlockStyle}" Width="*"/>

                <DataGridTextColumn Header="Start Date" Binding="{Binding startDate}" 
                            ElementStyle="{StaticResource WrapTextBlockStyle}" Width="*"/>

                <DataGridTextColumn Header="End Date" Binding="{Binding endDate}" 
                            ElementStyle="{StaticResource WrapTextBlockStyle}" Width="*"/>

                <DataGridTextColumn Header="Notes" Binding="{Binding notes}" 
                            ElementStyle="{StaticResource WrapTextBlockStyle}" Width="*"/>

                <DataGridTextColumn Header="Client ID" Binding="{Binding clientId}" 
                            ElementStyle="{StaticResource WrapTextBlockStyle}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Selected view area -->
        <Grid Background="#FF90C0EA" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="101*"/>
                <ColumnDefinition Width="173*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="90*"/>
                <RowDefinition Height="265*"/>
                <RowDefinition Height="61*"/>
                <RowDefinition Height="34*"/>
            </Grid.RowDefinitions>

            <Label Content="Contract Editor" Grid.ColumnSpan="2" Background="#00000000" Foreground="#FF1B3D69" FontSize="24" FontFamily="Cascadia Mono SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <StackPanel Grid.Row="1" Grid.Column="1">
                <TextBox Text="{Binding ContractIDInput}" Margin="20,20,20,0" Padding="5" IsReadOnly="True"/>
                <xctk:DateTimePicker Margin="20,20,20,0" Padding="5" Value="{Binding StartDate}"/>
                <xctk:DateTimePicker Margin="20,20,20,0" Padding="5" Value="{Binding EndDate}"/>
                <ComboBox Margin="20,20,20,0" Padding="5"
                ItemsSource="{Binding Clients}" 
                SelectedItem="{Binding ClientID}"
                DisplayMemberPath="id"
            />
            <TextBox 
                AcceptsReturn="True"
                TextWrapping="Wrap"
                VerticalScrollBarVisibility="Auto"
                Text="{Binding Notes}"
                Grid.Row="1"
                Grid.Column="3"
                Margin="18,18,18,18" Height="49"
            />
            </StackPanel>

            <StackPanel Grid.Row="1" Grid.Column="0">
                <Label Content="ID:" Margin="0,19,0,0" Padding="5" Background="#00000000" Foreground="White" FontSize="14"/>
                <Label Content="Start Date:" Margin="0,19,0,0" Padding="5" Background="#00000000" Foreground="White" FontSize="14"/>
                <Label Content="End Date:" Margin="0,19,0,0" Padding="5" Background="#00000000" Foreground="White" FontSize="14"/>
                <Label Content="Client ID:" Margin="0,19,0,0" Padding="5" Background="#00000000" Foreground="White" FontSize="14"/>
                <Label Content="Notes:" Margin="0,19,0,0" Padding="5" Background="#00000000" Foreground="White" FontSize="14"/>
            </StackPanel>

            <Grid Grid.Row="2" Grid.ColumnSpan="2" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150*"/>
                    <ColumnDefinition Width="150*"/>
                </Grid.ColumnDefinitions>
                <Button  Command="{Binding DeleteCommand}" Content="Delete"  Grid.Column="0" Margin="15"/>
                <Button  Command="{Binding UpdateCommand}" Content="Update"  Grid.Column="1" Margin="15"/>
            </Grid>
        </Grid>

    </Grid>
</UserControl>
