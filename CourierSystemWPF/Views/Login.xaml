<UserControl x:Class="CourierSystemWPF.Views.Login"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="1" Grid.Row="0" Text="Courier System Login" FontSize="24" FontWeight="Bold" Foreground="#FF232156" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,20" FontFamily="Cascadia Mono SemiBold" />

        <!-- Username -->
        <StackPanel Grid.Column="1" Grid.Row="1" Margin="0,10">
            <TextBlock Text="Username" FontWeight="SemiBold" Margin="0,0,0,5" />
            <TextBox Name="usernameTxt" Text="{Binding UsernameInput}" Padding="8" FontSize="14" BorderBrush="#CCCCCC" BorderThickness="1" Background="White" />
        </StackPanel>

        <!-- Password -->
        <StackPanel Grid.Column="1" Grid.Row="2" Margin="0,10">
            <TextBlock Text="Password" FontWeight="SemiBold" Margin="0,0,0,5" />
            <TextBox Name="passwordTxt" Text="{Binding PasswordInput}" Padding="8" FontSize="14" BorderBrush="#CCCCCC" BorderThickness="1" Background="White"/>
        </StackPanel>

        <Button Grid.Column="1" Grid.Row="3" Content="Login" Command="{Binding LoginCommand}" Margin="0,20,0,0" Height="40" Background="#007ACC" Foreground="White" FontWeight="Bold" BorderBrush="#007ACC" HorizontalAlignment="Stretch" />

        <!-- Status Text -->
        <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding StatusText}" Foreground="Red" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,20,0,0" />
    </Grid>
</UserControl>